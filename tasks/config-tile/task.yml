---
platform: linux

image_resource:
  type: docker-image
  source: {repository: smatyukevich/docker-cfbosh}

inputs:
  - name: pcf-pipelines

params:
  VAULT_ADDR: 
  VAULT_TOKEN: 
  FOUNDATION_NAME:

run:
  path: pcf-pipelines/tasks/config-tile/task.sh
